2012-01-19  Giuseppe Attardi  <attardi@di.unipi.it>

	* Classifier.h (Tanl::Classifier): added virtual estimate() method.

2009-09-09  Antonio Fuschetto <fuschett@di.unipi.it>

	* GIS.cpp (GIS::train): added an explicit cast (from int to double) of a
	"log" function argument; changed index type of a loop (from int to
	unsigned) to resolve a warning; changed allocation of "prob" array
	from automatic to dynamic;
	(GIS::update) changed index type of a loop (from int to unsigned) to
	resolve a warning;

	* LBFGS.cpp (LBFGS::estimate): added "isfinite" function alias on Windows;
	replaced "numeric_limits<double>::max()" function with "DBL_MAX" constant.

	* MaxEnt.cpp (MaxEnt::estimate): changed allocation of "numFeats" array
	from automatic to dynamic.

	* GIS.h (GIS:read): removed "read" function definition.

2009-05-01  Antonio Fuschetto  <fuschett@di.unipi.it>

	* LBFGS.cpp: added #include <limits> directive (according to new C++
	standard).

	* MaxEnt.cpp: added #include <limits.h> and #include <algorithm>
	directives (according to new C++ standard).

2009-03-21  Giuseppe Attardi  <attardi@di.unipi.it>

	* GIS.cpp (train): introduced variant for using the standard
	correction. This turns out more accurate for NER.
	It is enabled defining variable SLACK in MaxEnt.h.

	* MaxEnt.cpp (readEvents): moved here from GIS.cpp and LBFGS.cpp.

2009-03-20  Giuseppe Attardi  <attardi@di.unipi.it>

	* MaxEnt.cpp (buildIndex): significant speed up by using a
	hash_map for indexing events.

2009-03-19  Giuseppe Attardi  <attardi@di.unipi.it>

	* GIS.cpp (train): reintroduced use of alpha in update.

	* MaxEnt.cpp (save): use VISIBLE format.

	* test/train.cpp (main): fixed reading parameters with -gis.

	* test/Makefile.in: fixed dependencies.

	* GIS.h (~GIS): removed: already done in ~MaxEnt().
	(class GIS): removed lambda, already defined in MaxEnt.
	
	* GIS.*, LBFGS.*: moved readEvent(), counter, pID to MaxEnt.

2009-03-10  Giuseppe Attardi  <attardi@di.unipi.it>

	* Event.h (Features::add): dropped extra string constructor.

2009-03-06  Giuseppe Attardi  <attardi@di.unipi.it>

	* LBFGS.cpp (readEvents): allow multiple calls before calling train().

	* Event.h (class GenericEvent): added. Event is instanciated with
	type Features.

2009-03-03  Giuseppe Attardi  <attardi@di.unipi.it>

	* GIS.cpp (update): renamed ON_EACH -> TO_EACH

2009-03-01  Giuseppe Attardi  <attardi@di.unipi.it>

	* Event.h (Features::add): pass argument as reference.

2008-02-23  Giuseppe Attardi  <attardi@di.unipi.it>

	* samples/Makefile.in (train): removed FLIBS

2008-01-26  Giuseppe Attardi  <attardi@di.unipi.it>

	* configure.ac: changed option --disable-32bit to --enable-32bit

2007-09-22  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/LBFGS.cpp: removed DEBUG print.

	* model/lbfgsAlg.cpp (mcsrch): added initialization to brackt.

2007-07-20  Giuseppe Attardi  <attardi@di.unipi.it>

	* samples/predict.cpp (main): changed to read data from file since
	gcc 4.0 fails reading from cin (conflicts with Model::load()).

2007-03-30  Giuseppe Attardi  <attardi@di.unipi.it>

	* SentenceSplitter/SSEventStream.cpp (next): fixed handling of nextlen

	* SentenceSplitter/SSEventStream.h (class SSEventStream): fixed
	memory leak.

2007-02-03  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/Model.h (verbose): added verbose parameter to control
	whether to print progress indication.

2006-12-09  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/LBFGS.cpp (buildIndex): fixed creation of predOutcomes.

2006-09-22  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/LBFGS.cpp (save): added LBFGS algorithm

2006-02-04  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/GIS.cpp (readEvents): optionally dump events to stdout

2006-01-28  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/Model.cpp (loadZhang): added for loading model files
	generated by Zhang Maxent Toolkit.
	(http://homepages.inf.ed.ac.uk/s0450736/maxent_toolkit.html)

	* model/Model.h (hash<pair<unsigned, unsigned>): better randomize
	using a shift, or else all diagonal pairs will be mapped to 0 and
	symmetric pairs will be mapped to same value.

2006-01-27  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/Model.h (hash<pair<unsigned, unsigned>): 
	perform xor of two words.
	Previous version was flaky since gcc optimizer -O4 would discard
	the operation.

2005-11-23  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/GIS.h: added destructor ~GIS().

2005-04-19  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/Model.h (hash::operator()): specialize to unsigned.

2005-04-15  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/GIS.cpp (buildIndex): replaced find() with simple linear scan
	of outcomes to detect duplicates: was using 30% of runtime.
	Now buildIndex() takes 86% of runtime.

2005-04-11  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/GIS.h: handle hash_map in GNUC 3.

2005-04-09  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/GIS.cpp (update): turned expected into a vector<vector<pair>>.
	expected[pID] contains pairs <c, exp_prob>, for each class to which
	predicate pID contributes.
	This avoids having to scan the map for all possible classes,
	as in previous version.

2004-08-27  Giuseppe Attardi  <attardi@di.unipi.it>

	* samples/BasicEventStream.cpp (next):
	outcome in first position

2003-08-29  Giuseppe Attardi  <attardi@di.unipi.it>

	* SentenceSplitter/SSContextStream.cpp (next): fixed handling of
	features.add(nextEQ + rest)

2003-08-08  Giuseppe Attardi  <attardi@di.unipi.it>

	* model/util/HtmlReader.cpp (hasNext): is->gcount() - 1,
	since gcount() includes \n.

2003-08-07  Giuseppe Attardi  <attardi@di.unipi.it>

	* SentenceSplitter/SSContextStream.cpp (ContextStream): added
	version that reads text between start and end
	
	* MaxEntropy/model/util/TextReader.cpp (hasNext): modified to use
	new version of next_token()

2003-07-04  Giuseppe Attardi  <attardi@di.unipi.it>

	* MaxEntropy/SentenceSplitter/SSStream.cpp:
	removed "') from sentence marks.

2003-06-30  Giuseppe Attardi  <attardi@di.unipi.it>

	* MaxEntropy/model/ModelGIS.cpp: new compact data format for
	GIS model file

2003-06-27  Giuseppe Attardi  <attardi@di.unipi.it>

	* Applications/Paragraph/ParagraphInfo.{h,cpp}: modified to use
	MaxEntropy/SentenceSplitter
